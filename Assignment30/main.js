var username = ['eric', 'willie', 'admin', 'erin', 'ever'];
var usernames = (username);
if ("username = admin") {
    console.log("Hello " + username + ", thank you for logging in again!");
}
else {
    console.log("Hello admin, would you like to see a status report?");
}
